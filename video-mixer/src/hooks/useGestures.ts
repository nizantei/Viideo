// Placeholder for gesture hooks - will be implemented in Phase 6
// Currently gestures are handled directly in components

export function useGestures() {
  // Stub for now
  return {
    bind: () => ({}),
    resetZoom: () => {},
  };
}
